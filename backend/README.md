# To install packages and dependencies before start: 
    From backend folder , run command : 
    "npm install"

# To start backend with nodemon:
    From backend folder, run command :
     "npm run devStart"
